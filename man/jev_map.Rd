% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{jev_map}
\alias{jev_map}
\title{Evaluate shared questions for multiple independent states}
\usage{
jev_map(
  states,
  questions,
  provider = c("typesafe", "openrouter"),
  model = NULL,
  concurrency = 4L,
  timeout = 30,
  max_retries = 3L,
  rate_limit = 5,
  retry_budget = 120,
  on_result = NULL,
  progress = interactive()
)
}
\arguments{
\item{states}{A character vector or list of complete states. A named input
supplies durable state IDs; unnamed inputs receive positional IDs.}

\item{questions}{A named list of questions or a \code{jev_spec()} object.}

\item{provider}{Provider to use.}

\item{model}{Requested model, or the provider default when \code{NULL}.}

\item{concurrency}{Maximum number of active HTTP requests per wave.}

\item{timeout}{Maximum seconds for one HTTP attempt.}

\item{max_retries}{Number of retries after the first attempt.}

\item{rate_limit}{Preventive requests-per-second throttle.}

\item{retry_budget}{Maximum seconds for retries and waits for one state.}

\item{on_result}{Optional caller-owned callback for each terminal result.}

\item{progress}{Display the httr2 progress indicator.}
}
\value{
A \code{jev_result_set} preserving input order and IDs.
}
\description{
Evaluate shared questions for multiple independent states
}
\details{
\code{jev_map()} represents many independent evaluations. It does not combine
multiple states into one model request. Requests are admitted in bounded
waves, while \code{rate_limit} controls preventive admission separately from
\code{concurrency}.

For a valid response envelope with invalid individual answers, the result
has \code{status = "partial"}, keeps only validated answers in \code{response$answers},
and records per-question errors in \code{question_errors}. The request ledger's
\code{duration_seconds} is the per-response HTTP total reported by
\code{httr2::resp_timing()}; it is \code{NA} when that timing is unavailable. The
ledger timestamps are \code{NA} because \code{req_perform_parallel()} returns after
the HTTP work and does not expose reliable per-request wall-clock stamps.
}
\examples{
\dontshow{if (identical(Sys.getenv("JEVR_RUN_EXAMPLES"), "true") && nzchar(Sys.getenv("TYPESAFE_API_KEY"))) withAutoprint(\{ # examplesIf}
states <- list(
  incident_a = list(message = "Payouts fail", days = 3),
  incident_b = list(message = "Refund pending", days = 1)
)
questions <- list(
  urgent = jev_noul("Does this request convey urgency?")
)
results <- jev_map(
  states = states,
  questions = questions,
  provider = "typesafe",
  concurrency = 2
)
results
\dontshow{\}) # examplesIf}
}
